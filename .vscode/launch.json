{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "SimpleScript Launch",
            "type": "simplescript",
            "request": "launch",
            "program": "${workspaceRoot}/vs/test.ss"
        }
    ]
}